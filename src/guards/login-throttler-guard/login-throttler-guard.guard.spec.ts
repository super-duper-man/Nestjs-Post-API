import { LoginThrottlerGuardGuard } from './login-throttler-guard.guard';

describe('LoginThrottlerGuardGuard', () => {
  it('should be defined', () => {
    expect(new LoginThrottlerGuardGuard()).toBeDefined();
  });
});
